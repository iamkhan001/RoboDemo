<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/dm_80dp"
    android:orientation="vertical">

    <TextView
        android:id="@+id/hotel_activity_item_text"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dm_61dp"
        android:layout_gravity="center"
        android:drawableEnd="@drawable/arrow"
        android:gravity="center"
        android:paddingEnd="@dimen/dm_30dp"
        android:text="2018-2-2"
        android:textColor="@color/hotel_activities_new_list_text_color"
        android:textSize="@dimen/dm_30dp" />
</RelativeLayout>